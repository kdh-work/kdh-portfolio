# 260919_handbook

권다혜 포트폴리오 프로젝트 이해·유지보수 핸드북

- 작성일: 2026-09-19
- 기준: 문서 갱신 전 Git HEAD `c3d43e6`, 현재 소스와 설정, 기존 작업 기록
- 대상: 처음 프로젝트를 읽는 사람, 다음 작업을 이어받는 개발자, 기능 구조를 설명하려는 작성자
- 이 문서는 구현 안내서다. 경력·프로젝트 소개 전문은 [HANDBOOK.md](./HANDBOOK.md), 빠른 실행은 [README.md](./README.md)를 참고한다.

## 1. 어떤 프로젝트인가

B2B 엔터프라이즈 콘솔을 개발해 온 프론트엔드 개발자의 개인 포트폴리오다.
핵심 메시지는 “설정이 많은 화면을 조합으로 정리합니다”이다. 경력을 나열하는 데서
그치지 않고, 복잡한 설정 조건을 정리한 구조와 자원 관계를 시각화한 결과를 직접
조작할 수 있게 제공한다.

사이트에 소개하는 실무 프로젝트는 CMP, VISTA, KB IQ+ DevOps, 삼성 SCP OpenStack,
건설공제 정보시스템의 5건이다. 개인 프로젝트는 Robot Fleet Console과 My Games의
2건이다. 이 저장소에는 해당 제품 전체가 아니라 포트폴리오 설명·스크린샷과 세 업무 데모, 개인 수면 실험이 들어 있다.

위저드는 벤더를 추상화한 예제이고 관계도 자원 이름·주소·식별자는 가상 데이터다.
데모를 실제 서비스 API와 연결된 관리 콘솔로 해석하면 안 된다.

## 2. 화면을 읽는 순서

| 화면 | 확인할 내용 | 구현 진입점 |
| --- | --- | --- |
| 메인 | 경력 → 설정 구조 사례 → 관계도 → 실무·개인 프로젝트 → 기술·연락 | `src/app/page.tsx` |
| 설정 위저드 | 조합 선택 → 조건별 설정 → 검증과 완료 | `src/app/demo/page.tsx` |
| 자원 관계도 | 자원 계층, 회전·이동·확대, 렌더러 차이, 설계 개선 기록 | `src/app/iso-map/page.tsx` |

실제 URL에는 `/kdh-portfolio`가 붙는다. 예를 들어 위저드는
`http://localhost:3000/kdh-portfolio/demo/`다.

메인의 텍스트는 `src/content`에서 시작해 읽으면 된다. 화면 배치는
`components/sections`, 공통 헤더·푸터는 `components/layout`, 스크린샷과 확대 보기는
`components/media`에 있다. 실무·개인 프로젝트 목록은 `ProjectList`를 공유한다.

## 3. 기술 구성과 경계

| 항목 | 현재 구성 |
| --- | --- |
| 실행 환경 | `.nvmrc` 기준 Node 22 |
| 프레임워크 | package.json 기준 Next.js `^16.3.4`, React `19.0.0` |
| 언어 | TypeScript `5.7.3`, strict·noUncheckedIndexedAccess |
| 스타일 | 전역 디자인 토큰 + CSS Modules, Pretendard CDN |
| 시각화 | 기본 SVG, 비교용 Three.js `^0.170.0` 지연 로드 |
| 배포 | 정적 export → `out/` → GitHub Pages |
| 검사 | `typecheck`, `build`; 별도 테스트·린트 스크립트 없음 |

의존성의 범위 선언과 실제 설치 버전은 다를 수 있다. 재현 설치에는
`package-lock.json`과 `npm ci`를 사용한다.

이 저장소에는 서버 API나 데이터베이스 구현이 없다. WebView를 위한 메시지 타입과
클라이언트 래퍼는 있지만 네이티브 앱 셸은 없다. 브라우저에서는 네이티브 전송이 무동작이다.

## 4. 설정 위저드의 동작 원리

### 데이터 흐름

`자원 유형·벤더 선택 → registry.resolveSections → SectionForm/FieldControl → useWizard 검증 → DonePanel`

| 계층 | 역할 | 파일 |
| --- | --- | --- |
| 자원 유형 | 컴퓨트·네트워크·스토리지별 섹션 목록 결정 | `model/registry.ts` |
| 조합 | 유형과 벤더 조합별 컴포넌트 결정, 지원 여부 판정 | `model/registry.ts` |
| 입력값 | build(values)로 현재 값에 맞는 필드 구성 | `model/registry.ts` |
| 검증 | 필드별 형식과 교차 검증 규칙 | `model/rules.ts` |
| 상태 | 단계·조합·값·오류·확인 대기·로그 관리 | `model/useWizard.ts` |
| 화면 | 탭·폼·확인창·로그·완료 화면 | `ui/` |

위 경로는 `src/features/settings-wizard/` 아래다. 섹션 구성은 상태로 중복 저장하지
않고 현재 조합과 입력값으로 계산한다. 메인의 조합 미리보기와 데모는 같은
`CombinationMatrix`와 registry를 사용한다.

### 사용자가 경험하는 규칙

1. 지원되는 자원 유형·벤더 조합을 선택한다.
2. 조합에 맞는 입력 항목이 나타난다. 상위 선택에 따라 하위 필드가 달라진다.
3. 이미 입력한 상태에서 조합이나 의존 필드를 바꾸면 초기화 확인을 받는다.
4. 취소하면 기존 입력을 유지하고, 확인하면 영향을 받는 값을 정리한다.
5. 저장 시 현재 구성된 필드만 검증하고 성공하면 완료 화면에 입력 결과를 표시한다.

확인 대기는 콜백 대신 `PendingIntent` 객체로 표현한다. “조합 변경”과 “상위 필드 변경”을
구분하므로 확인·취소 처리의 의도를 상태에서 확인할 수 있다. 텍스트는 입력 중 draft를
유지하다 포커스를 벗어날 때 확정하고, 선택형 입력은 즉시 반영한다.

**저장은 서버 전송이 아니다.** `submit`은 검증 후 단계 상태를 3으로 바꾼다.
새로고침 후 복원하는 영속 저장도 없다.

## 5. 자원 관계도의 동작 원리

### 데이터에서 화면까지

`content/isoMap.ts의 가상 자원 → vpc/buildVpcLayout.ts → IsoScene → SVG 또는 WebGL`

| 경계 | 책임 | 주요 파일 |
| --- | --- | --- |
| scene | 도메인과 무관한 투영·장면 타입·핀·텍스트 기하 | `types.ts`, `projection.ts`, `pins.ts`, `text.ts` |
| vpc | VPC 자원 구조를 장면으로 변환, 배치·라벨·색 결정 | `sourceTypes.ts`, `buildVpcLayout.ts` |
| ui | 표시 모드·배율·시점·선택 및 실제 렌더링 | `VpcIsoMap.tsx`, `IsoMap.tsx`, `three/IsoMapThree.tsx` |

위 경로는 `src/features/iso-map/` 아래다. SVG는 투영된 path를 그리고 WebGL은
투영 전 `solid` 좌표를 카메라로 투영한다. 배치 규칙은 같은 함수에 있지만 래퍼는
현재 투영, solid, 회전 프레임 크기를 얻기 위해 함수를 여러 번 호출한다.

`solid`는 시점·배율과 독립적으로 메모이제이션한다. 회전할 때마다 지오메트리와
글자 텍스처를 불필요하게 다시 만들지 않기 위한 경계다. 두 렌더러가 같은 배치를
사용해도 화면 정렬과 갱신 타이밍은 별도 검증 대상이다.

### 조작 방법

| 입력 | 결과 |
| --- | --- |
| 3D에서 일반 드래그 | 시점 회전 |
| Space + 드래그 | 맵 이동 |
| Space + 휠 | 커서 위치를 기준으로 확대·축소 |
| + / − 버튼 | 단계별 배율 변경 |
| 배율 값 드래그 | 연속 배율 변경 |
| 배율 값 클릭 | 전체 맞춤 |
| 배율 값 포커스 후 방향키·+/− | 단계별 배율 변경 |
| 배율 값 포커스 후 Home·Enter·Space | 전체 맞춤 |

일반 휠은 페이지 스크롤에 사용한다. 배율은 기본 10~400% 범위이며 전체 맞춤에
필요하면 한계가 조정된다. 100%는 도식 좌표와 CSS 픽셀의 1:1을 뜻한다.

### 렌더러별 차이

| 기능 | SVG | WebGL |
| --- | --- | --- |
| 3D 시점 | 투영식으로 계산 | 직교 카메라로 계산 |
| 평면도 | 지원 | 선택하더라도 평면도는 SVG 사용 |
| 핀 배치 | 공통 pins 사용 | 같은 pins를 오버레이로 표시 |
| 핀 클릭 | 상세 정보가 있으면 카드 펼침 | 직접 자원 선택 콜백 |
| 상세 카드 | 지원, 화면 px 기준 크기 유지 | 현재 미구현 |
| 자원 박스 키보드 접근 | DOM 기반 지원 | 동일 수준 구현은 남은 작업 |
| 구획 이름 | SVG 텍스트 | 캔버스 텍스처 |

평면도에서 핀 설정은 텍스트 표시로 바뀌고 3D로 돌아오면 원래 선택이 복원된다.
자원 선택은 실제 상세 페이지 이동 대신 `IsoMapStage` 아래의 패널로 전달된다.

기본 SVG를 유지하고 WebGL을 비교용으로 둔 이유는 렌더링 비용뿐 아니라 글자,
이벤트, 접근성 구현 비용을 함께 비교하기 위해서다. 과거 번들 크기·좌표 오차
측정값은 기존 README와 검토 기록에 있지만 이번 문서 작업에서 재측정하지 않았다.

## 6. 작업 이력과 유지해야 할 해결책

아래는 기능 도입 순서와 Git 커밋을 대조한 요약이다.

| 작업 | 확인 가능한 이력 | 의미 |
| --- | --- | --- |
| 자원 관계도 기능 병합 | `5625101` | SVG 기반 관계도와 비교 구현 기반 |
| 콜아웃 핀·상세 카드 | `0a0af6d`, `f4af71f` | 이름을 박스에서 분리하고 정보 확인 경로 추가 |
| 두 렌더러 핀 공유·평면 라벨 개선 | `7f69972` | 표시 일관성과 긴 이름 처리 |
| 개발 루트 리다이렉트 | `1645b33` | localhost 루트에서 basePath로 진입 |
| WebGL 확대 중 핀 떨림 수정 | `2d2a481` | 페인트 전 크기·카메라 동기화 |
| 첫 진입 빈 화면·핀 이동 충돌 수정 | `ec7f476` | effect 순서와 포인터 이벤트 경계 수정 |
| 검토 기록 보강·후속 병합 | `52b432e`, `53d2ea8` | 결함과 확인 방법을 설명에 반영 |
| 콘솔 도메인 표현·콘텐츠 핸드북 | `739f852`, `c3d43e6` | 소개 내용과 저장소 문서 보강 |

특히 다음 결정을 유지한다.

- WebGL 생성 → 지오메트리 → 크기·카메라 처리는 같은 종류의 `useLayoutEffect`에서 선언 순서대로 수행한다. 일부만 일반 effect로 바꾸면 첫 렌더가 비거나 확대 시 어긋날 수 있다.
- 회전 포인터 캡처는 누르는 즉시가 아니라 끌기 임계를 넘은 뒤 시작한다. 즉시 캡처하면 클릭 대상이 바뀌어 자원 선택이 막힐 수 있다.
- 카드 크기는 화면 px, 핀은 도식 좌표 기준이다. 카드까지 배율에 비례시키면 확대 시 캔버스를 넘는다.
- 구획 이름은 배경판으로 덮지 않고 윤곽선에 빈 구간을 만들어 넣는다. 구획 여백·모서리 반경을 바꾸면 이름과 모서리 충돌을 확인한다.
- 회전 프레임은 모든 각도의 좌표 범위 합집합이 아니라 최대 크기와 현재 중심을 이용한다.
- `.wrap`과 컴포넌트 여백 클래스를 같은 요소에 합치지 않는다. 프로덕션 CSS 순서에서 padding이 덮일 수 있다.
- SVG `title`은 하나의 문자열로 만든다. 분리 텍스트 노드의 SSR 주석으로 인한 불일치 이력이 있다.

## 7. 실행과 배포

저장소 루트는 `polio/portfolio`다. 상위 `polio`에는 인수인계와 초기 자료가 있다.

```bash
cd /Users/kdh/Desktop/Mine/polio/portfolio
nvm use
npm ci
npm run dev
```

`nvm`을 쓰지 않는 환경은 Node 22를 먼저 준비한다. 개발 서버는 같은 디렉터리에
중복 실행하지 않는다. 접속 주소는 `http://localhost:3000/kdh-portfolio/`이며 개발
모드에서 루트 `/`로 접속해도 이 경로로 이동한다.

```bash
npm run typecheck
npm run build
```

`next.config.ts`는 `output: "export"`, `trailingSlash: true`,
`images.unoptimized: true`를 사용한다. 정적 산출물은 `out/`에 생성된다.
정적 결과 검증은 basePath를 보존해 서빙해야 하며 `npm run start` 대신 정적 파일
서버를 사용한다. 개발 서버와 배포 빌드의 스타일 적용 순서는 별도로 확인한다.

`.github/workflows/deploy-pages.yml`은 main 푸시 또는 수동 실행 시 설치·빌드 후
Pages에 배포한다. 현재 workflow에는 별도 typecheck 단계가 없다.
저장소 Pages 설정의 소스는 GitHub Actions여야 한다.

설정상 공개 주소는 [GitHub Pages 포트폴리오](https://kdh-work.github.io/kdh-portfolio/)다.
이번 문서 작업은 원격 배포 상태를 조회하거나 배포를 실행하지 않는다.

## 8. 변경할 때 찾아갈 곳

| 바꾸려는 내용 | 수정 지점 | 함께 확인할 것 |
| --- | --- | --- |
| 소개·경력 | `src/content/profile.ts` | 기간·요약·기존 콘텐츠 핸드북 |
| 실무·개인 프로젝트 | `src/content/workProjects.ts`, `personalProjects.ts` | 스크린샷 경로와 크기 |
| 사례 연구·데모 소개 | `src/content/caseStudy.ts`, `demo.ts` | 실제 위저드 동작과 설명 일치 |
| 위저드 조합·필드 | registry·types·rules | 기존 값 초기화와 현재 필드 검증 |
| 관계도 샘플 | `src/content/isoMap.ts` | 가상 데이터 유지, 예외 사례 보존 |
| 관계도 배치 | `vpc/buildVpcLayout.ts` | SVG·WebGL·평면도·긴 이름 |
| 확대·이동 | `ui/useMapViewport.ts` | 커서 기준 확대·맞춤·핀 위 이동 |
| 회전 | `ui/useYawDrag.ts`, `scene/projection.ts` | 클릭과 드래그 구분·가시면·라벨 |
| 이력서 PDF | profile.docsHref와 SiteFooter | PDF 블록 주석 해제도 필요 |
| 배포 경로 | `next.config.ts` | withBasePath·이미지·직접 하위 경로 접속 |

이미지 경로에는 `src/lib/paths.ts`의 `withBasePath`를 사용한다. `next/link`는
basePath를 처리하지만 이미지 src는 별도 처리한다. 전역 Git 사용자·인증 설정은
바꾸지 않고 이 저장소의 로컬 설정을 유지한다.

## 9. 검증 체크리스트

문서 수정은 링크·경로·소스 일치 여부를 확인한다. 코드 변경 시에는 영향 범위에
맞춰 typecheck와 build를 실행하고 아래의 관련 동작을 확인한다.

- 메인: 섹션 앵커, 이미지 확대 보기, 데모 진입과 복귀, 좁은 화면 배치.
- 위저드: 지원 조합, 조건 필드 변경, 초기화 확인·취소, 오류 필드 이동, 단계 복귀 시 값 유지, 완료 후 다시 시작.
- 관계도: SVG/WebGL 첫 진입, 회전, 평면도 전환, 라벨 전환, 빈 데이터와 긴 이름.
- 핀: SVG 카드 열기·닫기·자원 선택, WebGL 직접 선택, 핀 위 Space + 드래그.
- 확대: 버튼·값 드래그·휠·전체 맞춤, 확대 중 도형과 핀 정렬, 카드 잘림.
- 배포 산출물: basePath의 이미지 경로, 직접 `/demo/`·`/iso-map/` 접속, 프로덕션 여백.

포인터 캡처와 브라우저 합성 클릭 문제는 `dispatchEvent`만으로 확인할 수 없다.
실제 포인터 입력을 사용한다. 타입 검사는 이 상호작용을 검증하지 않는다.

## 10. 남은 작업과 문서 관리

현재 확인되는 미완료 범위는 다음과 같다. 이 목록은 새 작업의 승인이나 우선순위를 뜻하지 않는다.

- 이력서·경력기술서 PDF 연결과 푸터 노출.
- 순수 모델 테스트 자동화: resolveSections, rules, projection, buildVpcLayout, 핀·텍스트 기하.
- WebGL 키보드 접근·텍스처 글자 대체 설명. 카드 등 SVG와의 기능 차이를 어디까지 맞출지도 결정 필요.
- 네이티브 WebView 셸, 뒤로 가기·safe-area·공유 연동.
- 숨겨 둔 “아직 경험하지 않은 영역”의 재노출 여부 결정.

평면도 SVG 사용과 비교용 WebGL 유지는 현재 설계다. WebGL 평면도를 반드시
추가해야 하는 확정 요구사항으로 간주하지 않는다.

문서는 다음 역할로 나눠 관리한다.

| 문서 | 역할 |
| --- | --- |
| README.md | 실행·구조·현재 범위의 짧은 진입 문서 |
| 260919_handbook.md | 이 날짜의 프로젝트 구조·기능·유지보수 안내 |
| HANDBOOK.md | 사이트 소개 문구 모음, 원본은 src/content와 컴포넌트 |
| 상위 HANDOFF.md | 로컬 환경·과거 결정·재발 방지 기록, 저장소 외부 |
| 상위 portfolio-react-README.md | 초기 이식 자료, 현재 상태의 기준 아님 |

이번 문서는 소스·설정·Git 이력을 대조해 작성했다. 브라우저 기능 검사, 빌드,
타입 검사, 과거 성능 수치 재측정은 이번 문서 갱신의 검증 범위에 포함하지 않았다.


## 추가 작업 — 개요 화면 인터랙션과 개인 수면 실험

- 라우트: `/resource-overview`, `/experiments/liquid-bar` (공통 basePath 적용).
- 콘텐츠: `src/content/resourceOverview.ts`, `liquidExperiment.ts`. CMP의 역할·성과·링크는 `workProjects.ts`, 경력 요약은 `profile.ts`.
- 화면: 기존 디자인 토큰과 `DemoFrame.module.css`를 공유하는 `StudyFrame`·`StudyNotes`.
- 모델/UI: `features/resource-overview/{model,ui}`, `features/liquid-bar/{model,ui}`. 새 외부 의존성 없음.
- 업무 자료의 실제 이름·수치·API를 복사하지 않고 별도 예제로 구성. 실무 레이아웃 전체나 상태 판정 기준을 직접 설계했다고 서술하지 않는다.
- 확인 항목: 필터 합계 유지, 0건 결과, 칩 강조·상세, 포인터 드래그, 배치 되돌리기·초기화·방식별 복원, 좁은 화면 한 열, 수면 정지·재생.
- 빌드 검증: Node 22에서 정적 export 성공. 배포는 별도이며 이 변경에서 수행하지 않음.
