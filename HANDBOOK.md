# 포트폴리오 핸드북

권다혜 포트폴리오 사이트에 실제로 실려 있는 내용을 한 문서로 모은 것이다.
면접 전 훑어보기, 지원처별 버전 만들기, 이력서·경력기술서로 옮겨 적기에 쓴다.

- 공개 주소 — https://kdh-work.github.io/kdh-portfolio/
- 최종 갱신 — 2026.09
- 문서 기준 — `src/content/*` 의 데이터와 섹션 컴포넌트의 고정 문구
- 문구를 고칠 때는 이 문서가 아니라 `src/content` 를 고친다. 화면이 원본이고 이 문서는 사본이다.

프로젝트 구조와 유지보수 안내는 [260919_handbook](./260919_handbook.md)을 참고한다.

---

## 1. 사이트 구성

| 경로 | 내용 |
| --- | --- |
| `/` | 포트폴리오 본문 — 히어로 · 경력 요약 · 조합 매트릭스 미리보기 · 사례 연구 · 자원 관계도 미리보기 · 실무 프로젝트 · 개인 프로젝트 · 사용 기술 · 연락 |
| `/demo` | 설정 위저드 데모 — 조합을 고르면 2단계 구성이 결정되는 흐름을 직접 조작 |
| `/iso-map` | 자원 관계도 데모 — 회전·확대·이동, 렌더러 전환, 검토 기록 4종 |
| `/resource-overview` | 자원 현황 바·필터링, 섹션 배치·상하 이동의 공개용 축약 데모 |
| `/experiments/liquid-bar` | 개인 표현 실험 — 스크롤에 반응하는 SVG 수면 |

상단 내비게이션: 경력 · 사례 연구 · 자원 관계도 · 실무 프로젝트 · 개인 프로젝트 · 사용 기술 · 연락

---

## 2. 첫 화면 (Hero)

- **이름 · 직무** — 권다혜 / 프론트엔드 개발자
- **한 줄** — 설정이 많은 화면을 조합으로 정리합니다
- **리드** — 통합 모니터링과 클라우드 관리 플랫폼에서 장치·플랫폼 설정 영역을 담당했습니다. 조건이 얽혀 사용자가 판단하기 어려운 화면을, 앞단의 선택 하나가 이후 구성을 결정하는 구조로 바꿔왔습니다.

요약 지표 네 가지:

| 지표 | 설명 |
| --- | --- |
| 4년 9개월 | 2021.12 ~ 재직 중 |
| Vue 3 · TypeScript | 실무 주력 |
| React · Next.js | 개인 프로젝트 |
| B2B 엔터프라이즈 콘솔 | 담당 도메인 |

---

## 3. 경력 요약

### 오케스트로㈜ — 2022.07 ~ 재직 중 · CMP실 인벤토리2팀 · 주임

| 프로젝트 | 기간 | 역할 |
| --- | --- | --- |
| OKESTRO CMP 클라우드 통합 관리 솔루션 | 2026.01 ~ 진행 중 | 플랫폼 연결 설정 · 개요 화면 인터랙션 · 자원 관계도 · 신규 기능 기획·설계 검토 |
| 하나금융TI 통합 모니터링 솔루션 VISTA | 2023.07 ~ 2026.03 | 장치·플랫폼 통합 설정 파트 리드 |
| KB국민은행 IQ+ DevOps 시스템 구축 | 2023.10 ~ 2024.01 | 소스 코드 관리 · 테스트 코드 메뉴 |
| 삼성 SCP OpenStack 플랫폼 구축 | 2022.07 ~ 2023.04 | 클라우드 모니터링 유저 콘솔 |

### 대보정보통신㈜ — 2021.12 ~ 2022.06 · 전략기술팀 · 사원

| 프로젝트 | 기간 | 역할 |
| --- | --- | --- |
| 건설공제 고객 중심 정보시스템 고도화 | 2022.02 ~ 2022.06 | 고객 관리 파트 SI |

### 다섯 가지 사실

- **담당 도메인** — 클라우드 관리 · 통합 모니터링 · DevOps 콘솔. 경력 전체가 B2B 엔터프라이즈 웹 콘솔과 데스크톱 환경입니다. 권한에 따라 화면이 분기되는 단일 콘솔과 접속 경로가 분리된 콘솔을 모두 담당했습니다.
- **일하는 방식** — 얽힌 도메인 규칙을 화면 구조로 정리하고, 공통 기반과 판단 근거를 문서로 남깁니다. 기존 코드를 이어받아 구현하며 드러난 문제를 개선하는 일이 많았고, 최근에는 기획·설계 단계부터 참여해 구현 가능성과 API·데이터 구조를 미리 검토합니다.
- **AI 활용** — 작업의 크기와 불확실성에 따라 활용 방식을 나눕니다. 작은 수정은 자연어로 요청해 바로 고치고, 처음 다루는 기술은 제 방법과 AI 제안을 비교해 판단하며, 큰 작업은 규칙·계획·검토 기준을 먼저 세운 뒤 진행합니다. 화면 시안은 여러 안을 빠르게 만들어 비교할 때 씁니다. 최종 설계와 품질 판단은 직접 합니다.
- **주력 스택** — Vue 3 Composition API와 TypeScript로 4년, 개인 프로젝트에서 React 19와 Next.js 15로 약 1년.
- **학력** — 동국대학교 컴퓨터공학전공 학사 (2018.03 ~ 2022.02)

---

## 4. 사례 연구 — 설정 구조를 두 번 다시 설계한 과정

> 하나금융TI 통합 모니터링 솔루션 VISTA에서 모니터링 대상 장치·플랫폼 통합 설정 파트를 담당했습니다. 이 화면은 한 번에 지금 구조가 된 것이 아니라, 제안하고 구현한 개선안을 스스로 기각한 뒤 다시 설계한 결과입니다.

### 4.1 세 단계

**① 원안 — 설정 항목마다 한 단계** *(기획 단계에서 결정된 구조)*
설정 항목이 많아 항목별로 단계를 나눈 구성. 최대 7단계였고 모든 사용자에게 7단계가 그대로 노출됐다.

**② 1차 제안 — 단계를 동적으로 구성** *(제안 · 구현)*
자원 유형에 따라 필요하지 않은 단계가 있다는 점을 확인. 기획자와 단계별 조건을 함께 정리한 뒤 자원 유형에 따라 단계가 동적으로 구성되도록 제안·구현.

**③ 재설계 — 필요한 설정을 한 화면에 모으기** *(직접 기각 후 재제안 · 구현)*
반복 테스트와 QA 이슈를 확인하면서, 단계 수가 아니라 **단계를 나누는 구조 자체**가 사용을 어렵게 만든다고 판단. 목표를 단계 축소가 아니라 "설정을 한 화면에 모으는 것"으로 바꾸고, 그 화면 구성을 무엇이 결정할 수 있는지 역으로 찾았다. 답은 **자원 유형과 벤더 조합**. 이 조합을 1단계로 올려 확정하면 2단계는 조합의 결과를 펼치는 자리가 된다. 구현 전 단계에서 유보하셨던 기획자도 이 근거를 받아들였다.

### 4.2 판단 근거

> 구현 이후 반복 테스트를 하면서 QA에 등록되는 이슈를 함께 확인했습니다. 이슈가 특정 화면의 결함으로 흩어져 있지 않고 단계를 이동하는 흐름에서 반복된다는 점이 판단의 근거였습니다. 개별 이슈에 대응하는 대신, 발생 원인인 단계 분리 구조 자체를 바꾸는 쪽을 제안했습니다.

*단서 — 당시 이슈를 별도로 기록해두지 않아 건수는 밝히지 않는다. 기획 변경 이후 같은 유형의 이슈가 뚜렷하게 줄어든 것까지가 확인된 사실이다.*

### 4.3 구현 — 조건이 해석되는 세 층

조건을 세 층으로 나누고, **해석되는 시점이 다르다**는 점을 기준으로 처리 위치를 분리했다.

| 층 | 조건 | 처리 위치 |
| --- | --- | --- |
| 확정 | 자원 유형 → 섹션 목록 | 1단계 선택으로 확정되므로 조회 데이터로 관리 |
| 확정 | 자원 유형 × 벤더 → 섹션별 컴포넌트 | 같은 시점에 확정되므로 조합 표에서 조회해 주입 |
| 반응 | 필드 값 → 하위 필드 구성 | 입력 중 계속 바뀌므로 런타임 계산으로 격리 |

**초기화 규칙** — 하위 필드는 상위 필드가 옵션 목록을 채우는 관계여서, 상위 값이 바뀌면 기존 하위 값이 유효하지 않게 된다. 값을 조용히 버리지 않도록 초기화 안내를 먼저 띄우고 확인을 받은 뒤 초기화한다. 같은 원칙이 단계 사이에도 적용되어, 1단계 조합을 바꾸면 2단계 설정 전체가 초기화된다. 상위 선택이 하위 값의 유효 범위를 정의한다는 하나의 규칙을 층마다 같게 적용해, 조건이 복잡해도 동작이 예측 가능하도록 했다.

### 4.4 결과

- **7단계 → 2단계** — 설정 단계
- **250 line → 53 line** — 단계 진행 컴포넌트를 3개 화면에 공통 적용해 Step 처리 코드
- **벤더 추가 시 화면 수정 불필요** — 화면 코드에서 조건 분기를 제거하고 조합 데이터로 이동해

---

## 5. 데모 ① 설정 위저드 (`/demo`)

**조합이 화면 구성을 결정하는 설정 위저드**

- 통합 모니터링 솔루션에서 담당했던 장치·플랫폼 설정 화면의 구조를 재현한 데모. 1단계에서 자원 유형과 벤더를 확정하면, 2단계에는 그 조합에 필요한 섹션과 입력 항목만 구성된다.
- 단계 사이 이동은 자유롭다. 초기화 안내는 1단계에서 조합을 실제로 바꿀 때, 2단계에서 상위 필드를 바꿀 때만 나타난다. 오른쪽 패널에 어떤 조건이 어느 시점에 해석됐는지 기록된다.
- **고지** — 실무 코드를 사용하지 않고 새로 구현했다. 벤더명과 설정 항목은 모두 일반화한 가상 값이며, 실제 프로젝트의 도메인 데이터는 포함되어 있지 않다.

데모 데이터 — 자원 유형 3종(컴퓨트 · 네트워크 · 스토리지) × 벤더 3종(벤더1 · 벤더2 · 벤더3).
섹션은 기본 정보 · 인증 정보 · 수집 설정 · 수집 대상 · 임계값 · 볼륨 범위 중 자원 유형이 고르며, 성립하지 않는 조합은 빗금으로 막아 둔다.

**메인 페이지 미리보기 문구** — "조합을 고르면 화면 구성이 결정됩니다". 실무에서는 이 조합 표가 화면 코드가 아니라 별도 데이터로 관리되어, 새 벤더가 추가될 때 화면을 수정하지 않아도 되도록 했다.

---

## 6. 데모 ② 자원 관계도 (`/iso-map`)

**계층을 그대로 읽히게 만든 자원 관계도**

- 클라우드 관리 플랫폼에서 VPC 자원의 관계를 보여주는 화면. 표로 나열하면 어느 서브넷이 어느 라우팅 테이블을 거쳐 어느 게이트웨이로 나가는지 눈으로 따라갈 수 없어서, 계층이 한 방향으로만 흐르는 도식으로 바꿨다.
- 3D 는 WebGL 이 아니라 **아이소메트릭 투영**이다. 좌표 계산만으로 만들어지므로 글자는 그대로 글자이고, 박스는 키보드로 이동할 수 있는 요소로 남는다. 끌면 시점이 돌아가는데 이것도 투영 각도 하나를 바꾸는 것이다 — 회전 후에도 여전히 아핀 변환이기 때문.
- 자원 이름은 박스 위에 얹지 않고 **콜아웃 핀**으로 띄운다. 이름 길이가 박스 크기를 좌우하지 않고, 라벨이 겹칠 때는 정보를 버리는 대신 지지대를 늘려 비켜 세운다. 핀을 누르면 상세 카드로 펼쳐진다.
- **고지** — 표시되는 자원 이름·주소·식별자는 전부 가상 값. AWS 자원 유형(VPC · 서브넷 · 라우팅 테이블 · 게이트웨이)은 공개된 개념이라 그대로 두었다.

### 관계도가 해석하는 세 가지 관계

| 종류 | 관계 | 표현 |
| --- | --- | --- |
| 포함 | 가용영역 → 서브넷 | 가용영역으로 묶어 한 열에 세로로 쌓아, 연결선이 다른 박스를 관통하지 않게 |
| 직속 | VPC → 라우팅 테이블 | 서브넷 열 오른쪽 별도 레인. 어떤 서브넷이 쓰는지는 연결선으로 |
| 참조 | 라우팅 테이블 → 네트워크 자원 | VPC 경계 밖에 두고 파선으로 이어, 포함 관계가 아님을 선 모양으로 구분 |

### 검토 기록 ⓐ 회전·확대·이동

열다섯 번을 돌았고 문제 제기는 전부 화면을 쓰는 쪽에서 나왔다. 원인이 증상과 어긋나 있던 것들만 남겼다.

- 깜빡임 — 원인이 **둘**이었다(회전 중 호버 재판정 / 구역 이름이 90°마다 다른 변으로 점프, 최대 442px).
- 한 방향으로만 회전 — 회전 코드가 아니라 슬라이더가 값을 자르는 문제. 각도는 순환하는 값이라 선형 컨트롤로는 양 끝을 이을 수 없다.
- 각도마다 컨테이너 높이 재설정 — 경계 상자가 각도에 따라 변해 높이가 598~1141px(변동폭 91%)로 움직였다. 고정 창으로 잡고 높이를 CSS 로 못 박음.
- 확대 후 상하로만 이동 — flex 가운데 정렬은 시작 쪽 넘침에 스크롤이 닿지 않는다. 자식의 `margin: auto` 로 교체.
- 좌우 이동 불가 — 표 셀의 max-content 폭 때문에 넘침 자체가 사라졌다. `contain: inline-size` 로 해결. **컴포넌트를 어디에 넣는지가 동작을 바꾼 경우.**
- 맵마다 100% 의 기준이 다름 — 패치 대신 100% 의 뜻을 먼저 정의. 언제나 1:1 로 두고 하한을 맞춤 배율에 연동. 범위가 10~400% 로 넓어져 끌기 감도를 덧셈에서 곱셈으로 변경.

**같은 결함을 세 번 만났다** — 최종 범인은 전역 CSS 리셋의 `svg { max-width: 100% }`. 지정 폭 1198.96px 가 실제 903px 로 눌리고 있었고, 리셋에 `max-height` 가 없어 폭만 눌리고 높이는 자랐다. 고침은 이 도식에만 `max-width: none` 을 돌려주는 한 줄.

교훈 다섯: 한 증상에 원인이 하나라고 가정하면 절반만 고친다 / 증상의 형태가 원인을 가리킨다 / 옳지만 불완전한 고침이 원인 추적을 늦춘다 / 격리된 하네스는 자기가 빼놓은 것 때문에 생기는 문제를 재현하지 못한다 / CSS 원인은 소스 검색으로 찾히지 않는다(규칙 7209개를 열거해 찾음).

### 검토 기록 ⓑ 자원 이름을 어디에 둘 것인가 (콜아웃 핀)

박스 윗면에 이름을 얹는 방식은 처음부터 어긋나 있었다 — 이름이 길수록 박스를 넓혀야 하고, 마름모 윗면에서는 가운데 두어도 한쪽으로 쏠려 보인다. 이후 13개 증상을 거쳤다(카드 잘림, 배율에 따른 떨림, WebGL 전환 시 도식이 사라짐, Space+드래그 충돌 등).

교훈: 정지 상태에서 재면 안 보이는 결함이 있다 / 나중에 얹은 것이 먼저 있던 규칙에 구멍을 낸다 / 실행 순서는 적어 둔 차례가 아니라 단계가 정한다 / 합성 이벤트는 브라우저 규칙(포인터 캡처 리타기팅)을 건너뛴다 / 무규칙해 보이는 간헐적 실패는 경쟁 조건을 의심한다 / 도식의 일부인가 그 위에 뜬 UI 인가 — 좌표계를 어디에 둘지는 이 구분에서 나온다.

### 검토 기록 ⓒ 구획 이름 배치 — 다섯 안

| 안 | 결과 |
| --- | --- |
| 꼭짓점에 맞추기 | 기각 — 레일이 꼭짓점에 닿기 전부터 휜다 |
| 모서리를 각지게 | 기각 — 어긋남은 사라지나 바닥/볼륨을 모양으로 구분하던 신호를 잃는다 |
| 변 가운데에 명판 | 기각 — 판 폭이 글자 수에 끌려다닌다 |
| 글자 외곽선으로 가리기 | 기각 — 획 사이로 레일이 비치고 덮는 색이 배경에 의존한다 |
| **레일을 실제로 끊기** | **채택** — 윤곽선 path 를 이름 자리만큼 비워 가릴 것을 없앰 |

배치 규칙이 레이아웃 상수를 되돌아 바꾼 사례 — 이름이 들어갈 빈 구간이 모서리 곡선을 침범하지 않으려면 변이 충분히 길어야 해서, 구획 여백을 0.55 → 0.8 로 넓혔다.

### 검토 기록 ⓓ SVG · WebGL 렌더러 비교

같은 어댑터가 만든 하나의 배치를 두 렌더러로 그리고 토글로 전환한다. 카메라를 SVG 투영과 같아지도록 유도해 **노드 11개의 가로 위치 차이가 0.00px**.

| 항목 | SVG | WebGL (Three.js) |
| --- | --- | --- |
| 번들 | 0KB | gzip 121KB, 지연 로드. 즉시 로드 시 초기 스크립트 187KB 에서 65% 증가 |
| 글자 | 전부 `<text>`, 선택·검색·스크린리더가 닿음 | 박스 이름은 HTML 오버레이, 핀은 SVG 오버레이. 바닥에 눕는 구획 이름은 캔버스 텍스처 — 그 순간 글자가 아니게 됨 |
| 선 굵기 | `stroke-width` 한 줄 | 선 굵기가 없어 띠 메시를 직접 생성 |
| 같은 평면 겹침 | 그린 순서대로 | z-fighting. 데칼을 띄우고 polygonOffset 으로 해결 |
| 클릭·호버 | DOM 이벤트, 키보드 포커스 무료 | 레이캐스터. 키보드 접근은 별도 구현 |
| 시점 회전 | 투영식의 각도 하나 | 카메라 방위각 하나 (비용 동일) |
| 화면 개수 | 제한 없음 | WebGL 컨텍스트 상한(대개 16개) |

**결론** — 만들어 보고 나서도 원본 문서와 같다. 이 규모의 위젯에는 SVG 가 맞다. 값을 하는 지점은 원근·조명·수천 개 인스턴스다.

덤으로 확인된 것: 아이소메트릭은 직교 카메라의 특수한 경우이며 고각은 35.264°(=asin(tan30°)). 두 렌더러의 화면 좌표 차이는 1e-13 px. `SPAN_X · cos(35.264°) = 1` 이므로 원본 도식은 진짜 아이소메트릭보다 박스를 1.294배 높게 그려 왔다(의도된 과장이며 3D 에서도 재현).

---

## 7. 실무 프로젝트

> 경력 전체가 B2B 엔터프라이즈 웹 콘솔과 데스크톱 환경입니다. 기존 코드와 운영을 이어받아 점진적으로 개선하는 성격의 프로젝트가 많았습니다.

### 7.1 OKESTRO 멀티 하이브리드 클라우드 통합 관리 솔루션 CMP
**2026.01 ~ 진행 중 · 오케스트로 · 60명 규모**
플랫폼 연결 설정·관리, 개요 화면의 자원 현황·섹션 배치 인터랙션, 자원 관계도 프론트엔드. 신규 기능은 기획·설계 단계부터 참여해 구현 가능성과 API·데이터 구조를 검토하고, 개발 단계의 구현까지 담당

- 신규 기능 기획 회의에 참여해 구현 가능 여부를 프론트엔드 관점에서 미리 검토 — 기존 화면·공통 컴포넌트 구조와의 충돌, 현재 데이터 구조로 표현할 수 있는지, 추가 API와 화면 단 가공 필요 여부, 기존 기능 영향과 기술적 제약
- 필요한 데이터가 기존 API에 없거나 구조를 바꿔야 할 때 수집·백엔드 담당자와 직접 협의 — 신규·확장 API 여부, 요청 파라미터와 응답 구조, 필드 의미와 생성 위치, 가공 위치와 방식. 화면에 필요한 형태만 요청하지 않고 수집 → 백엔드 → API → 화면 전체 흐름에서 역할을 나누고, 기존 화면과 신규 화면의 데이터 기준을 맞춤
- 신규 화면은 요구사항과 기능 목적을 정리한 뒤 AI로 여러 형태의 HTML 목업을 만들고, 구조·사용성·기존 화면과의 일관성을 직접 비교해 장점을 조합하고 아이디어를 보태 2~3개 후보로 좁힌 뒤 팀장 리뷰로 방향 결정. 현재 설계 단계이며, 개발 단계에서 실제 프로젝트 구조와 컴포넌트에 맞춰 구현 예정
- 기획에서 정의한 개요 화면 레이아웃을 바탕으로, 자원 현황의 바형 표현·필터링과 섹션 배치·이동의 상세 동작을 설계·구현
- 상태 요약 선택 → 필터링 → 관련 자원 유형 강조로 이어지는 흐름을 구성. 요약은 전체 기준을 유지하고 목록은 선택 수량 / 전체 수량으로 표시해 조회 범위를 구분
- 섹션 그리드 배치·폭 조절과 상하 순서 이동을 비교 목업으로 구현하고, 방식별 배치 저장·초기화·조작 규칙을 핸드북으로 정리
- 기획 문서와 변경 이력이 부족한 상태에서 중도 합류해, 기존 코드와 API 흐름으로 기능 기준을 역으로 정리한 뒤 개발을 이어받음
- 표로 흩어져 있던 VPC 자원 관계를 계층이 한 방향으로 흐르는 아이소메트릭 도식으로 재설계. 시점 회전·확대·맵 이동을 WebGL 없이 좌표 투영만으로 구현해 글자와 키보드 접근성을 그대로 유지
- 같은 도식을 WebGL(Three.js)로도 만들어 렌더러 선택 근거를 예상이 아닌 실측으로 대체 — 번들 121KB 증가, 한글 라벨의 텍스처화, 선 굵기·클릭 판정 재구현 비용을 확인하고 SVG 유지로 결론
- 자원 이름을 박스 위 텍스트에서 콜아웃 핀으로 옮겨, 이름 길이가 박스 크기를 좌우하던 문제와 시점 회전 시 라벨 겹침을 함께 해소
- 간헐적으로만 재현되던 클릭 불능의 원인을 포인터 캡처의 이벤트 리타기팅으로 특정. 합성 이벤트로는 재현되지 않아 검증 방식 자체를 실제 포인터 입력으로 변경
- 값이 바뀌는 동안에만 어긋나는 결함(확대 중 레이어 불일치)을 연속 측정으로 특정. 고침이 다시 초기화 순서를 뒤집는 것까지 확인해 검토 기록으로 남김
- 플랫폼별 데이터 단위와 환산 기준을 분석해 공통 기준으로 정리, 화면 간 일관성 확보
- API 응답과 화면 상태가 어긋나는 문제를 UI 상태 동기화 구조로 개선
- 기능 기준과 변경 이력을 문서화해 반복 확인 비용 절감

`Vue 3 · TypeScript · TanStack Query · Pinia · Yup · Ant Design Vue · ECharts · vue-grid-layout · pnpm · GitLab · Jira · Confluence`

### 7.2 하나금융TI 통합 모니터링 솔루션 VISTA
**2023.07 ~ 2026.03 · 하나금융TI · 22명 규모**
모니터링 대상 장치·플랫폼 통합 설정 파트 리드, 프론트엔드 — *상세는 4장 사례 연구*

- 설정 구조 재설계로 등록 단계 7단계 → 2단계
- 공통 컴포넌트 라이브러리를 대부분 직접 설계·구현하고 프로젝트 후반 약 1년간 유지보수 담당
- 화면 기획 개선 제안으로 동일 API 중복 호출을 2회 이상에서 1회로 단축
- 클라우드 플랫폼 라우팅을 SPA 구조로 전환해 초기화·URL 이동 이슈 해결
- Infinite scroll + virtual scroll 과 pagination 성능 비교 후, 데이터 규모에 따라 편차가 큰 전자보다 일관된 인터랙션 성능을 내는 후자를 운영 기준으로 판단 (체크박스 전체 선택 소요 시간 최대 72.3% 단축)
- 프론트엔드 인수인계 문서 중 통합 파트 전체 작성 (v1.1 2025.07 / v1.2 2025.12)
- 사용자 교육이 제공되지 않는 환경을 전제로, 별도 안내 없이 다음 행동을 판단할 수 있는 구조를 기준으로 설계

`Vue 3 · TypeScript · TanStack Query · Pinia · PrimeVue · ECharts · Vite · Yarn Berry · MSW · ESLint · Husky · Bitbucket · Jira`

### 7.3 KB국민은행 IQ+ DevOps 시스템 구축
**2023.10 ~ 2024.01 · KB국민은행 · 약 100명 규모**
소스 코드 관리 · 테스트 코드 메뉴 담당, 프론트엔드

- 미구현 상태로 인수한 화면을 완성하고 API 바인딩 수행, 추가 개발이 필요한 기능을 식별해 공유
- 사용자 권한별 화면 분기 처리 및 권한 조합에 따른 이슈 시나리오 도출
- 기획과 고객 요구사항이 어긋난 부분을 주도적으로 협의해 기획 수정
- 개발이 불가한 설계에 대해 근거를 정리해 전달하고 상세 설계 보완 요청
- 단기 대응과 장기 개선 방안을 구분해 제안, 담당 화면의 후속 작업을 문서로 정리해 인수자에게 전달

`Vue 3 · JavaScript · Pinia · Ant Design Vue · ECharts · npm · GitLab`

### 7.4 삼성 SCP OpenStack 플랫폼 구축
**2022.07 ~ 2023.04 · 삼성SDS · 약 13명 규모**
클라우드 모니터링 유저 콘솔 프론트엔드

- OpenStack 전환으로 지원이 중단된 기능을 대체하고 변경된 데이터 구조에 맞춰 UI 재구성
- 삭제된 이벤트가 활성 상태로 남는 이슈의 원인을 데이터 구조·기능 흐름 기준으로 파악하고 해결 방법 제시
- 메트릭 단위 자동 환산 처리로 표시 영역을 벗어나는 이슈 해결
- 유효성 메시지 처리를 공통 함수로 분리해 재사용성 확보
- 테스트 시나리오 작성 및 QA 수행, 통과율 98%

`Vue 2 · JavaScript · ECharts · npm · pnpm · GitLab · Bitbucket`

### 7.5 건설공제 고객 중심 정보시스템 고도화
**2022.02 ~ 2022.06 · 건설공제조합 · 약 70명 규모**
고객 관리 파트 SI

- WebSquare 3 기반 화면을 WebSquare 5 로 마이그레이션, 기존 기능 유지 및 신규 요구사항 반영
- 3개월간 19개 화면 마이그레이션, 해당 화면 QA 이슈 5건
- API 바인딩 처리 및 실제 데이터와 설계 간 불일치 수정

`WebSquare 5 · Java · Eclipse · SVN`

---

## 8. 개인 프로젝트

> 실무는 Vue였고, React와 Next.js는 개인 프로젝트에서 기획부터 단독으로 진행하며 익히고 있습니다. 두 프로젝트 모두 진행 중입니다.

### 8.1 Robot Fleet Console — 로봇 플릿 관제 콘솔
**2026.06 ~ 진행 중 · 기획 · 설계 · 개발 단독**
실무 모니터링 경험을 기반으로 대규모 제조·물류 현장의 관제 시나리오를 직접 설계. 공장 4곳, 로봇 3종. 프론트엔드 약 4,350 line.

- 도메인 단위 features 아키텍처로 분리 (fleet / floor / sites / deployment / flow-map / monitoring)
- Next.js API Routes 를 Mock BFF 로 구성해 백엔드 의존 없이 화면·상태 구조 검증
- 폴링 훅을 상태 관리 라이브러리 없이 직접 구현 — 의존성 변경 시 이전 데이터 잔상 제거, ref 로 최신 fetcher 유지해 불필요한 재구독 차단, 화면 이탈 시 잔여 요청 정리
- OpenLayers 로 지리 좌표와 평면도 좌표 두 좌표계를 함께 처리
- 상태 판정·지표 정의·그래프 구성을 lib 계층으로 분리하고, 순수 함수 대상 단위 테스트 작성
- 로봇 개체 등록·수정은 도메인 지식 한계를 명시하고 범위에서 의도적으로 제외

`React 19 · Next.js 15 App Router · TypeScript 5.7 · OpenLayers 10 · Vitest 2 · Testing Library · CSS Modules · pnpm`

수록 화면 6장 — 거점 맵(`/sites`) · 플릿 대시보드 KPI(`/fleet`) · 공장×상태 히트맵(`/fleet`) · 공장 로봇 목록(`/fleet/[factoryId]`) · 공장 평면 맵(`/floor/[factoryId]`) · 배포 프로세스(`/deployments/[id]`).
*로컬 실행 화면이며 데이터는 Mock BFF 가 만든 가상 값.*

### 8.2 My Games — 플랫폼 통합 게임 라이브러리
**2026.05 ~ 진행 중 · 기획 · FE · BE · 로컬 인프라 단독**
외부 플랫폼에서 소유 게임을 가져와 저장하고, 수동 등록 게임과 함께 조회하는 라이브러리. 프론트엔드 약 4,000 line.

- 외부 API 호출과 DB 조회를 화면 단위로 분리 — 동기화 화면은 방금 받은 응답을 그대로 보여주고, 라이브러리 화면은 저장본만 조회해 재호출하지 않음
- 같은 게임 목록을 두 경로로 볼 수 있게 하되, 각 화면이 어느 데이터를 보고 있는지 상단에 명시해 혼동 방지
- 표시 언어 전환에 따라 플랫폼 API 의 로케일 파라미터를 함께 바꿔, 설명·에디션 정보를 해당 언어 응답으로 표시
- 앱 내 WebView 탑재를 전제로 반응형 구조 설계 — 화면 폭에 따라 아이콘 중심/텍스트 라벨 내비게이션 분기, 가로 스크롤 제스처가 뒤로 가기로 전환되는 현상 완화, 좁은 폭에서 레이아웃 붕괴 방지
- NestJS 와 Prisma 로 API 를 직접 구성하고 PostgreSQL 을 Docker 로 운영

`React 18 · TypeScript · Vite · React Router · TanStack Query · Zustand · Zod · Tailwind CSS · NestJS · Prisma · PostgreSQL · Docker`

수록 화면 5장 — 플랫폼 동기화(`/sync`) · 라이브러리(`/library`) · 게임 검색(`/steam-search`) · 상세 응답 한국어/English 비교 2장.
*계정 식별 정보는 예시 값으로 대체. 로컬 실행 화면이며 외부 플랫폼 데이터는 실제 응답.*

---

## 9. 사용 기술 정리

> 앞의 프로젝트에서 실제로 사용한 것만 적었습니다. 실무에서 운영까지 경험한 것과 개인 프로젝트에서 사용한 것을 구분하고, 아직 경험하지 않은 영역도 함께 표기합니다.

### 실무 · 4년 9개월

| 구분 | 내용 |
| --- | --- |
| 언어 | TypeScript, JavaScript |
| 프레임워크 | Vue 3 Composition API, Vue 2 |
| 상태 | TanStack Query, Pinia, Vuex |
| UI · 시각화 | Ant Design Vue, PrimeVue, ECharts, vue-grid-layout |
| 빌드 · 품질 | Vite, Webpack, Yarn Berry PnP, pnpm, ESLint, Prettier, Husky, MSW |
| 협업 | Jira, Confluence, GitLab, Bitbucket |
| AI 도구 | Claude(주력), Cursor |

### 개인 프로젝트 · 약 1년

| 구분 | 내용 |
| --- | --- |
| 프레임워크 | React 19 / 18, Next.js 15 App Router |
| 상태 | TanStack Query, Zustand, 커스텀 폴링 훅 직접 구현 |
| 검증 · 지도 | Zod, OpenLayers 10 |
| 스타일 · 테스트 | Tailwind CSS, CSS Modules, Vitest 2, Testing Library |
| 백엔드 · 인프라 | NestJS, Prisma, PostgreSQL, Docker |

### 아직 경험하지 않은 영역

*현재 화면 노출은 보류 상태다(`TechStack.tsx` 에 주석 처리). 문구는 `personalProjects.ts` 에 유지한다.*

- 네이티브 브릿지 연동 — WebView 환경을 전제로 한 반응형 설계까지만 경험했고, 브릿지 통신은 구현하지 않았습니다
- CI/CD 파이프라인 구축 — 파이프라인과 리뷰어 규칙이 있는 환경에서 프로세스에 참여했으나 구축은 담당하지 않았습니다
- 컴포넌트 테스트와 E2E — 순수 로직 단위 테스트만 작성했고, Cypress 와 Playwright 는 경험이 없습니다
- B2C 서비스와 모바일 네이티브 앱

---

## 10. 연락

- 이메일 — kdhtoon@gmail.com
- 거주 — 경기도 수원시
- 학력 — 동국대학교 컴퓨터공학전공 학사
- 이력서 · 경력기술서 PDF — **준비 중**. `profile.docsHref` 를 채우고 `SiteFooter.tsx` 의 주석을 풀면 연락 섹션에 링크가 나타난다.
- 공개 페이지에는 전화번호를 두지 않는다. 파일로 제출하는 이력서에만 기재한다.

---

## 11. 사이트 자체의 기술 사항

면접에서 "이 포트폴리오는 무엇으로 만들었나" 를 물었을 때 답할 내용.

- **스택** — Next.js 16 App Router · React 19 · TypeScript 5.7 · CSS Modules. 정적 빌드(`out/`) 후 GitHub Pages 에 배포. Three.js 는 자원 관계도의 WebGL 렌더러에만, 지연 로드.
- **린트 스크립트 없음** — `typecheck`는 `strict` + `noUncheckedIndexedAccess` 설정으로 타입을 검사한다. 린트나 동작 테스트를 대체하지 않는다.
- **콘텐츠와 화면 분리** — 포트폴리오 텍스트는 전부 `src/content` 에 데이터로 둔다. 지원처별 버전을 만들 때 컴포넌트를 건드리지 않는다.
- **3계층 경계를 디렉토리로** — 자원 관계도는 `scene`(도메인 무관) / `vpc`(어댑터, 도메인 지식이 여기에만) / `ui`(렌더러) 로 나뉜다. 다른 자원 관계도가 필요해지면 어댑터만 새로 쓴다.
- **두 렌더러가 하나의 배치를 그린다** — 어댑터가 같은 레이아웃을 SVG 용 화면 좌표(`IsoScene`)와 투영 전 그리드 좌표(`IsoScene.solid`) 두 형태로 내보낸다. 배치 규칙을 공유하며, 카메라·프레임·갱신 시점에 따른 화면 정렬은 별도 검증한다.
- **초기화 확인을 의도 객체로** — `useWizard` 는 확인 대기 상태를 콜백이 아니라 `PendingIntent` 값으로 들고 있다. 어떤 확인이 떠 있는지 상태만 보고 알 수 있다.
- **safe-area 변수** — `--safe-top` / `--safe-bottom` 은 브라우저에서 0 이고, WebView 에서 네이티브가 노치 값을 주입할 자리다.
- **basePath** — 사이트가 `/kdh-portfolio` 하위 경로에 놓여 `next.config.ts` 의 `basePath` 가 켜져 있다. `next/image` 의 `src` 에는 자동으로 붙지 않아 `lib/paths.ts` 의 `withBasePath` 를 쓴다.

### 남은 작업

- [ ] 이력서·경력기술서 PDF 연결
- [ ] 모델 계층 단위 테스트 (Vitest) — `resolveSections`, `rules`, `projection`, `buildVpcLayout`
- [ ] WebView 셸 연동: 안드로이드 뒤로 가기 ↔ 히스토리, safe-area 주입, 공유 시트


## 12. 자원 현황·섹션 배치와 개인 실험

### 업무 사례: `/resource-overview`

전체 레이아웃은 기획에서 정의했다. 직접 담당한 범위는 자원 현황의 바형 표현·필터링과
섹션 그리드 배치·상하 이동의 상세 동작 및 구현이다. 공개 데모는 가상 데이터와
축약한 배치 규칙으로 새로 만들었으며 제품 적용 완료나 실제 API 연동을 의미하지 않는다.

- 전체 합계와 필터 결과의 수량 기준 구분, 0건 상태와 명시적 전체 보기.
- 상태의 자원 유형 선택 → 필터 변경 → 해당 행 강조. 목록 선택 시 가상 유형 상세.
- 비율 막대의 작은 구간은 상태 카드로도 조작 가능.
- 배치 방식은 비교 목업. 공개판은 순서 이동과 반폭·전체 폭을 제공하며 두 방식의 저장값은 독립.

### 개인 표현 실험: `/experiments/liquid-bar`

업무 성과와 분리된 스크롤 반응형 SVG 수면 실험이다. 메인 개인 프로젝트 아래와
자원 현황 데모 말미에서 연결한다. 장식용 수면, 수위로 비율 표현, 폭으로 비율 표현을
비교한다. 텍스트는 고정하며 움직임 정지·세기 선택과 시스템 움직임 최소화를 지원한다.
운영 화면의 사용성이나 대규모 데이터 성능은 검증 결과로 주장하지 않는다.
